name: First-workflow
on: 
  push
jobs:
  hello-gitActions:
    runs-on: ubuntu-latest
    steps:
      - name: Grreting a new learner of gitHub Actions
        run: echo "Hello, Welcome to github Actions"
      - name: say bye for the day !!
        run: echo "leave for the day !!, bye !!"
      - name: multi line shell
        run: |
            echo "first line"
            echo "done"
